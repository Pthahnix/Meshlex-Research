# Development-only files (not for publishing)
.mcp.json
.env

# Claude Code runtime (can be reinstalled)
.claude/
.claude.json

# System profile files
.bash_logout
.bashrc
.profile
.cache/
.local/

# Paper corpus - 300+ markdown files, 33MB, can be regenerated
# Only the research documents (00-06) and material/ analysis are original work
.context/paper/

# Runtime logs (training logs tracked for review)

# Data (large files, can be regenerated)
data/

# Checkpoints (large files, backed up to HF)
checkpoints/

# Results: validation outputs tracked for user review

# Python
__pycache__/
*.pyc
*.egg-info/
